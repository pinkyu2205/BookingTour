<Window x:Class="ChatApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ChatApp – Connect" Height="500" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BgDeepBrush}"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="16" Background="{StaticResource BgDeepBrush}" 
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.6" Color="#000000"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title bar drag area -->
            <Border Grid.Row="0" CornerRadius="16,16,0,0" Background="Transparent"
                    MouseDown="TitleBar_MouseDown">
                <Grid>
                    <TextBlock Text="● ● ●" Foreground="{StaticResource TextMutedBrush}" 
                               VerticalAlignment="Center" HorizontalAlignment="Left" Margin="14,0"/>
                    <Button Content="✕" Click="CloseButton_Click"
                            HorizontalAlignment="Right" VerticalAlignment="Center"
                            Width="28" Height="28" Margin="8,0"
                            Background="Transparent" BorderThickness="0"
                            Foreground="{StaticResource TextMutedBrush}" FontSize="13" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bg" CornerRadius="6" Background="Transparent">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bg" Property="Background" Value="{StaticResource RedBrush}"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- Main content -->
            <StackPanel Grid.Row="1" Margin="40,10,40,40" VerticalAlignment="Center">

                <!-- Logo / Icon -->
                <Border Width="72" Height="72" CornerRadius="20" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#6C63FF" Offset="0"/>
                            <GradientStop Color="#A78BFA" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <TextBlock Text="💬" FontSize="34" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <TextBlock Text="Welcome Back" FontSize="24" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           HorizontalAlignment="Center" FontFamily="Segoe UI"/>
                <TextBlock Text="Sign in to join the chat room" FontSize="13"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" Margin="0,6,0,30" FontFamily="Segoe UI"/>

                <!-- Server -->
                <TextBlock Text="SERVER HOST" Style="{StaticResource LabelStyle}"/>
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="90"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="TxtHost" Style="{StaticResource DarkTextBox}"
                             Text="127.0.0.1" Grid.Column="0" Margin="0,0,8,0"/>
                    <TextBox x:Name="TxtPort" Style="{StaticResource DarkTextBox}"
                             Text="9000" Grid.Column="1"/>
                </Grid>

                <!-- Username -->
                <TextBlock Text="USERNAME" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="TxtUsername" Style="{StaticResource DarkTextBox}"
                         Margin="0,0,0,24" Height="44"
                         KeyDown="Input_KeyDown"/>

                <!-- Error message -->
                <TextBlock x:Name="TxtError" Text="" Foreground="{StaticResource RedBrush}"
                           FontSize="12" HorizontalAlignment="Center"
                           Margin="0,-16,0,10" Visibility="Collapsed" FontFamily="Segoe UI"/>

                <!-- Connect button -->
                <Button x:Name="BtnConnect" Content="Connect to Chat"
                        Style="{StaticResource PrimaryButton}"
                        Click="BtnConnect_Click" IsDefault="True"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
